diff a/code/game/gamemodes/events.dm b/code/game/gamemodes/events.dm	(rejected hunks)
@@ -61,9 +61,9 @@
 		S.power_change()
 	for(var/area/A in world)
 		if(!istype(A, /area/space) && !istype(A, /area/shuttle) && !istype(A,/area/arrival))
-			A.power_light = 1
-			A.power_equip = 1
-			A.power_environ = 1
+			A.power_light = TRUE
+			A.power_equip = TRUE
+			A.power_environ = TRUE
 			A.power_change()
 
 /proc/power_restore_quick()
